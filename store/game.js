const initialState = {
  targetPosition: {},
  isOver: false,
  timer: 60
};

export default function(state = { initialState }, action) {
  switch (action.type) {
    default:
      return state;
  }
}
